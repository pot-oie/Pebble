package com.pot.pebble.ui.theme

import androidx.compose.ui.graphics.Color

// Cozy Flat (纸面物理) 配色方案

// 背景色
val CozyPaperWhite = Color(0xFFFDFCF5)   // 羊皮纸暖白 (App 背景)
val PureWhite = Color(0xFFFFFFFF)        // 纯白 (卡片背景)

// 文字与线条
val CozyCharcoal = Color(0xFF2D2D2D)     // 炭黑 (主要文字/标题)
val CozyGrey = Color(0xFF8C8C8C)         // 灰色 (次要文字)
val CozyBorder = Color(0xFFE6E6E6)       // 浅灰 (默认边框)

// 状态色
val CozyGreen = Color(0xFF5F8D4E)        // 苔藓绿 (安全/专注/开关开启)
val CozyGreenBg = Color(0xFFF6FFF0)      // 浅绿背景 (专注态)

val CozyRed = Color(0xFFBC4749)          // 陶土红 (禁区/警告)
val CozyRedBg = Color(0xFFFFF5F5)        // 浅红背景 (激活态)

val CozyYellow = Color(0xFFF2C94C)       // 芥末黄 (奖励/宝石)