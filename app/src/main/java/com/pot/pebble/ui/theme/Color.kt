package com.pot.pebble.ui.theme

import androidx.compose.ui.graphics.Color

// 🌿 疗愈自然配色方案
val NatureBeige = Color(0xFFF5F5DC)      // 米白背景
val NatureSurface = Color(0xFFFFFFFF)    // 纯白卡片
val MossGreen = Color(0xFF8A9A5B)        // 苔藓绿 (强调色)
val EarthBrown = Color(0xFF5D4037)       // 泥土褐 (文字)
val SoftGray = Color(0xFF9E9E9E)         // 灰色 (未激活状态)
val AlertOrange = Color(0xFFD87D4A)      // 暖橙色 (黑名单激活时的点缀)